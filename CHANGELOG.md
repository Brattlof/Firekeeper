# Changelog

## Unreleased

First working shape, built against the Forever beta that opened on 17 September 2026.

- Camp planner: one object per player, no two objects with the same effect, and buffs the
  group's classes already cover are skipped.
- Shared one-hour cooldown tracked per character, adjusted for the Legacy perk Field
  Guide.
- Group sharing over the `FKPR1` addon message protocol, with a local-only fallback.
- Camp panel, chat announcements, and slash commands.
- API capability probe (`/fk caps`) and a data gap report (`/fk gaps`).
